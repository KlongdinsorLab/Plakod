export enum Gender {
     Male = "M",
     Female = "F",
}

export enum Airflow {
     OneHundred = 100,
     TwoHundred = 200,
     ThreeHundred = 300,
     FourHundred = 400,
     FiveHundred = 500,
     SixHundred = 600
}

export enum StatusGameSession {
     Active = "ACTIVE",
     Cancel = "CANCEL",
     End = "END"
}

export enum StatusBooster {
     Available = "AVAILABLE",
     Expire = "EXPIRE"
}

export enum VasScore {
     One = 1,
     Two,
     Three,
     Four,
     Five,
     Six,
     Seven,
     Eight,
     Nine,
     Ten
}